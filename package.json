{
  "name": "dotagentsprotocol-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "generate-og": "node scripts/generate-og.mjs",
    "build": "node scripts/generate-og.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:hub": "node scripts/build-hub.mjs",
    "deploy:hub": "node scripts/build-hub.mjs && wrangler pages deploy dist-hub --project-name=dotagentsprotocol-hub --branch=main"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.17.1",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
